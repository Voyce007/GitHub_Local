name: release
on:
  push:
    tags: ["v*.*.*"]
jobs:
  cicd:
    uses: Voyce007/cicd-tooling-architecture/.github/workflows/release.yml@v1
    with:
      services_config: ci-cd/services.yaml
      bundle_paths: "docker-compose.yml .env.example README.md CLAUDE.md scripts layers .claude ci-cd"
